
- name: python application link checker
  on: [push]
  jobs:
    job1:
      - name: first job
        runs-on: ubuntu-latest
        steps:
          - name: step one - checking out
            uses: action/checkout@v2
          - name: step two - 
            run: 
